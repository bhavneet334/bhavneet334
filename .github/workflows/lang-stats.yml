name: Language Stats Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check token access
        run: |
          curl -H "Authorization: Bearer $GH_TOKEN" https://api.github.com/user
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
